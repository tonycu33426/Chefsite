<%= form_for(@foodtype) do |f| %>
  <% if @foodtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@foodtype.errors.count, "error") %> prohibited this foodtype from being saved:</h2>

      <ul>
      <% @foodtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
